DECLARE
  X NUMBER;
  Z X%TYPE;
  EMPLE ADMCON_BANNER.IC_BANNER%TYPE;
BEGIN
  EMPLE:=100;
  
END;


/
/*una variable puede ser del mismo tipo de una columna por ejemplo
y no nos preocupamos por averiguar el tipo de la columna*/